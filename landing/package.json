{
  "name": "robot-audit-sri-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing optimizada para ROBOT AUDIT SRI",
  "scripts": {
    "fonts": "node build-fonts.mjs",
    "css": "tailwindcss -i src/input.css -o assets/styles.css --minify",
    "build": "npm run fonts && npm run css",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
